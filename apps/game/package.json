{
  "name": "@migue/game",
  "scripts": {
    "dev": "parcel watch src/index.html --dist-dir ${PUBLIC_PATH:=./public}",
    "build": "parcel build src/index.html --dist-dir ${PUBLIC_PATH:=./public}"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.3.1",
    "phaser": "^3.55.2",
    "socket.io": "^4.1.3"
  }
}
